using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SceneClearedSound : SoundController
{
    // Start is called before the first frame update
    void Start()
    {
        GetRef();
        PlayRune();
    }

   
}
